# FEATURE INVENTORY

このファイルは、`docs/ROADMAP.md` に記載された機能・課題・アイデアを棚卸しするための台帳です。

- ユーザーが今覚えているアイデアだけでなく、ROADMAPに埋もれた項目を拾うことが目的です。
- 実装順を確定するものではありません。
- 正式仕様は `PROJECT_SPEC.md`、進行中の優先順位は `ROADMAP.md` を参照してください。
- 状態は棚卸し時点の判断であり、後続確認で更新します。
- 迷った項目は削除せず `要確認` に入れています。

## 状態ラベル凡例

| ラベル | 意味 |
|--------|------|
| `実装済み` | MVP以上の実装が完了 |
| `一部実装` | 基盤や一部機能のみ実装済み |
| `未着手` | ROADMAPに記載があるが実装なし |
| `要設計` | 方針・設計が固まっていない |
| `後続拡張` | 既存MVP実装後の追加機能として記録 |
| `要確認` | ROADMAP出典が曖昧、または実装有無が不明 |
| `解消済み` | 対応不要と判断されたか統合された |

---

## A. コンテンツ発見 / トップ / Explore

### FI-001: トップページ新着コンテンツ枠

- 状態: `実装済み`
- ROADMAP出典: `docs/ROADMAP.md` 現在地サマリ「トップページ 新着コンテンツ枠MVP」（2026-05-14実装）
- 概要: トップページに最新公開ゲーム・素材・キャラクターを各3件表示するセクションを追加。
- 備考: 新着順のみ。ランキング/人気順/使用数順は FI-010〜012 参照。

### FI-002: Explore ゲーム混在表示

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 次にやる候補・後回し「Explore」スコープ制限メモ「ゲーム表示なし（現在は素材・キャラクターのみ）」
- 概要: `/explore` にゲームも混在表示させる。現時点は素材・キャラクターのみ。
- 必要な基盤: ゲーム検索API統合、カード種別判定。

### FI-003: Explore 素材専用/キャラ専用フィルタ強化

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 次にやる候補「Explore 検索/フィルタ追加 MVP」スコープ制限（contentType/primaryTag フィルタ・キャラクター専用フィルタは追加しない）
- 概要: Explore の種別フィルタ強化（contentType/primaryTag、Emotion 等）。

### FI-004: コンテンツ名称統一 - UI完全統一フェーズ後続

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 次にやる候補 1番「コンテンツ名称統一 - UI 完全統一フェーズ（後続）」
- 概要: 2026-05-13 名称ルールMVP後続として、見出し・タブ・ナビの統一、docs更新を段階実装。

### FI-005: 素材管理・キャラクター管理の検索/フィルタ統一polish

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 次にやる候補 1-1「素材管理・キャラクター管理の検索/フィルタ統一（polish）」
- 概要: `/my/assets` と `/my/characters` の入力補助・細かな項目配置・操作感の最終統一。

### FI-006: 所有者向け管理エリアの表現統一

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 次にやる候補 1-2「所有者向け管理エリアの表現統一」
- 概要: `/my/assets` と `/my/characters` の構成・表現・操作導線（作成・編集・削除・公開状態変更）を統一。

### FI-007: 公開側ギャラリー検索/フィルタ統一polish

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 次にやる候補 1-3「公開側ギャラリーの検索/フィルタ統一（polish）」
- 概要: `/assets` と `/characters` のフィルタ粒度・UI密度をさらに合わせる。

### FI-008: Meilisearch連携強化

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「アセット指標の検索連携（タグ検索との複合、Meilisearch 連携）」「Meilisearchインデックス運用強化」
- 概要: 指標ソートとの複合検索、インデックス運用自動化。

### FI-009: レコメンド/おすすめ

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「指標・分析」（間接記載）
- 概要: ユーザー向けのゲーム・素材・キャラクターのおすすめ表示。いいね/閲覧指標が基盤になる。

---

## B. ランキング / 指標 / 分析

### FI-010: ランキング/人気順/プレイ数順

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「ゲーム一覧・プロフィール」「人気順・プレイ数順」
- 概要: 公開ゲーム一覧で人気順・プレイ数順のソートを提供。

### FI-011: 今遊ばれているゲーム

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 現在地サマリ トップページ新着コンテンツ枠備考「今遊ばれている/今使われている系の実装は後続課題として維持」
- 概要: 直近X時間/日のプレイ数を基にした「今遊ばれているゲーム」表示。

### FI-012: 今使われているアセット

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 現在地サマリ トップページ新着コンテンツ枠備考（同上）
- 概要: 直近の参照数・採用数を基にした「今使われているアセット」表示。

### FI-013: アセット閲覧数 viewCount

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「指標・分析」「アセット閲覧数 viewCount MVP」
- 概要: `/assets/:id` 閲覧時に viewCount をインクリメント。一覧/管理画面は非対象（MVP）。

### FI-014: アセット使用数 usedInGameCount

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「指標・分析」「アセット使用数 usedInGameCount の定義と集計方針」
- 概要: ゲーム内で参照されているアセットの使用数集計。公開/非公開・削除時の扱いを設計する。

### FI-015: アセット指標ソート（お気に入り順/閲覧数順/使用数順/人気順）

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「指標・分析」「アセット指標ソート・ランキング」
- 概要: 公開アセット一覧でお気に入り数・閲覧数・使用数・総合人気順のソートを提供。

### FI-016: アセット指標の検索連携

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「指標・分析」「アセット指標の検索連携」
- 概要: タグ検索と指標ソートの複合検索、Meilisearch連携。

### FI-017: favoriteCount カラム化

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「指標・分析」「指標基盤強化（favoriteCount カラム化、ユニーク閲覧、イベントログ、作者ダッシュボード）」
- 概要: 現在のお気に入り数は都度 COUNT クエリ。専用カラムでキャッシュし集計コストを下げる。

### FI-018: ユニーク閲覧

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「指標・分析」「指標基盤強化（…ユニーク閲覧…）」
- 概要: ユーザー/IPベースの重複排除した閲覧カウント。

### FI-019: ユニークプレイ

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「指標・分析」（ユニーク閲覧と並列記載）
- 概要: ユーザーごとの重複排除したプレイカウント。

### FI-020: イベントログ

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「指標・分析」「指標基盤強化（…イベントログ…）」
- 概要: 閲覧・プレイ・お気に入り・採用などのユーザーアクション記録。指標集計・おすすめ基盤になる。

### FI-021: 作者ダッシュボード

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「指標・分析」「指標基盤強化（…作者ダッシュボード）」
- 概要: 作者向けの自分のゲーム・アセット・キャラクターの統計・閲覧数・使用数一覧。

### FI-022: 不正/連打対策

- 状態: `要設計`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「指標・分析」（間接記載、FI-020 イベントログと連動）
- 概要: viewCount/playCount/favoriteCount の水増し防止。レートリミット・ユニーク検証を要設計。

---

## B-2. ゲーム演出 / ビジュアルエフェクト

### FI-022a: 背景フィルター / 背景ぼかしMVP

- 状態: `実装済み（2026-05-15）`
- ROADMAP出典: （未出典、2026-05-15実装）
- 概要: AI生成背景などで細部の粗が気になる場合に、背景画像だけをぼかして「場所の雰囲気は分かるが細部は目立たない」表現を実現。ノード単位で背景画像に blur と dim オーバーレイを設定可能。blurPx（0-24px）と dimOpacity（0-60%）の2パラメータで制御。キャラクター・メッセージウィンドウ・colorFilter には影響しない。CSS filter + scale で背景画像端の透けを防止。テストプレイ・公開プレイで即座に確認可能。「保存して次のノードへ」で背景引き継ぎ時に backgroundFilter も維持。
- DB/API実装メモ:
	- Prisma: `GameNode.backgroundFilter`（Json）を追加（migration: `20260515100000_add_background_filter`）。
	- API: `GamesService.normalizeBackgroundFilter` で `blurPx` を `0..24`、`dimOpacity` を `0..60` に clamp し、`0/0` は `null` 正規化。
	- `upsertNode`（create/update）と `duplicate` の双方で `backgroundFilter` を正規化して保存/複製。
	- `GameAssetReference` / `GameCharacterReference` の集計対象は素材/キャラ参照のみで、`backgroundFilter` 自体は集計対象外。
- 後続候補: pixelate/preset/シーン単位デフォルト/背景フィルタープリセット（FI-022b参照）

### FI-022b: 背景フィルター拡張（pixelate/preset/シーン単位デフォルト）

- 状態: `後続拡張`
- 概要: MVPの blur + dim に加え、pixelate（モザイク化）やプリセット（vintage/warm/cool等）。シーン単位/ゲーム単位での既定値設定。背景フィルタープリセット集（よく使う組み合わせ）の保存・選択。

---

## C. クレジット / ライセンス / スタッフロール

### FI-023: スタッフロールUI MVP（基本）

- 状態: `実装済み`
- ROADMAP出典: `docs/ROADMAP.md` 実装済み「スタッフロールUI MVP」「スタッフロール速度設定MVP」「スタッフロール自動表示ON/OFF MVP」「スタッフロール終了時挙動MVP」「スタッフロール表示順カスタマイズMVP」
- 概要: スタッフロールの表示・速度・自動表示・終了時挙動・表示順をゲーム単位で設定可能。

### FI-024: GameCredit snapshot lock guard 強化

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「クレジット・ライセンス」「GameCredit snapshot lock guard 強化」
- 概要: `db:check-game-credit-snapshots` 運用強化・公開前/公開時ガード連携。

### FI-025: 公開前確認の使用箇所一覧拡張（ピンポイントフォーカス・一括修正・自動差し替え）

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「公開前確認の使用箇所一覧拡張（複数箇所一覧MVPは実装済み。将来課題: 入力フィールドそのものへのピンポイントフォーカス・一括修正・自動差し替え・5件超の展開/折りたたみUI）」
- 概要: 複数使用箇所一覧は実装済み。残り: 入力フィールドへのピンポイントフォーカス・一括修正・自動差し替え・5件超の折りたたみ。

### FI-026: 5件超の展開/折りたたみUI

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「5件超の展開/折りたたみUI」
- 概要: 公開前確認モーダルで使用箇所5件超の場合に展開/折りたたみを実装。

### FI-027: スタッフロール演出強化

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「スタッフロール演出強化（スキップ導線、終了時挙動の詳細オプション、カテゴリ別アニメーション、より凝った演出）」
- 概要: スキップ導線・カテゴリ別アニメーション・より凝った演出。

### FI-028: スタッフロール終了時挙動詳細オプション

- 状態: `一部実装`
- ROADMAP出典: `docs/ROADMAP.md` 実装済み「スタッフロール終了時挙動MVP」（stop/close/loop の3択実装済み）。後回し「スタッフロール拡張（終了時挙動の詳細オプション）」
- 概要: 現在は stop/close/loop の3択。詳細オプション（ループ回数制限など）は後続。

### FI-029: スタッフロール細かな速度カスタマイズ

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「スタッフロール拡張（…より細かな速度カスタマイズ…）」
- 概要: 現在は3段階プリセット。スライダー等による細かな速度カスタマイズ。

### FI-030: スタッフロール用BGM

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「スタッフロール拡張（…スタッフロール用BGM）」「スタッフロール用BGMは音声素材管理・権利表示・GameCredit・公開前確認との整合を前提に設計し、単独で先行実装しない」
- 概要: スタッフロール再生中のBGM連動。音声素材管理・クレジット設計と整合が前提。

### FI-031: 構造化ライセンス（CCライセンス等）

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「クレジット・ライセンス」「構造化ライセンス（CC ライセンス等）」
- 概要: 現在の `usageTerms` 自由入力に加え、CCライセンスなどの選択式構造化ライセンスを追加。

### FI-032: 公開前チェックUIの完全ミニマル化

- 状態: `後続拡張`（サマリーMVPは実装済み）
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「公開前チェックUIの完全ミニマル化（ヘッダー重大度バッジのフィルタ化等）」
- 概要: 公開前チェックUIの情報密度・フィルタ操作を整理しミニマルな操作感にする。サマリーカード（公開準備OK/注意あり/要修正）・件数・優先issue最大3件・issue一覧アンカー導線は 2026-05-14 に実装済み（FI-032a）。ヘッダー重大度バッジのフィルタ化等は後続拡張。

### FI-032a: 公開前チェックサマリーMVP

- 状態: `実装済み（2026-05-14）`
- 概要: 公開前チェックパネルの issue 一覧より上にサマリーカードを追加。全体状態（公開準備OK / 注意あり / 要修正）・severity別件数・カテゴリ別件数・優先 issue 最大3件・issue一覧アンカー導線を表示。DB/API 変更なし。自動修正・一括修正は未実装（将来課題）。

---

## D. 公開中編集 / 公開版安全性

### FI-033: 公開中編集時の再確認UX拡張（差分検出/重要変更のみconfirm）

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「公開中編集時の再確認UX拡張（差分検出、重要変更のみ確認、独自モーダル化、「今後表示しない」導線）」
- 概要: 現在は全保存で window.confirm。差分検出で重要変更のみconfirmに変更し、独自モーダル・今後表示しない導線を追加。

### FI-034: 差分検出（公開版影響の自動判定）

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「公開中編集の差分検出基盤（公開版影響の自動判定）」
- 概要: 保存前に変更内容を解析し、公開版に影響する変更か否かを自動判定する基盤。

### FI-035: 独自モーダル（window.confirm置き換え）

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「独自モーダル化」
- 概要: 現在の `window.confirm` を専用モーダルコンポーネントに置き換える。

### FI-036: 今後表示しない導線

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「『今後表示しない』導線」
- 概要: 公開中編集confirmや注意バナーに「今後表示しない」オプションを追加。

### FI-037: 公開版/下書き版分離

- 状態: `要設計`
- ROADMAP出典: `docs/ROADMAP.md` 次にやる候補 4番「公開版/下書き版分離」、後回し「公開版/下書き版分離（編集反映タイミング制御）」
- 概要: 公開中編集の安全性向上の本命。下書き版を別途保持し、タイミングを選んで公開版に反映する。

### FI-038: ノード追加/シーン追加へのconfirm拡張

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 現在仕様「将来課題」セクション「ノード追加・シーン追加へのconfirm拡張」
- 概要: 公開中ゲームでのノード追加・シーン追加時にもconfirmを表示する。

---

## E. アセット / キャラクター管理

### FI-039: Asset.visibility 拡張（public/private/unlisted）

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 次にやる候補 2番「Asset.visibility 拡張（unlisted など）」、後回し「アセット/キャラクター visibility」
- 概要: 現在の `Asset.isPublic` Boolean MVP を `public/private/unlisted` 等に拡張。設計要。

### FI-040: sourceAssetId / derivedFromAssetId（派生元追跡）

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「sourceAssetId / derivedFromAssetId による派生元追跡」
- 概要: リミックス・加工素材の派生元を追跡するフィールド。

### FI-041: 再アップロード/コピー問題対策（perceptual hash / audio fingerprint）

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「再アップロード/コピー問題への対策（perceptual hash / audio fingerprint は将来課題）」
- 概要: 同一素材の重複アップロード検出。画像は perceptual hash、音声は audio fingerprint。

### FI-042: カバー画像アップロード導線

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題として記録」「カバー画像アップロード導線」
- 概要: ゲームカバー画像を直接アップロードできる導線。現在は既存アセットから選択のみ。

### FI-043: トリミング/クロップ

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題として記録」「トリミング/クロップ」
- 概要: 画像アップロード時またはカバー画像設定時にブラウザ上でトリミング/クロップを行う。

### FI-044: 推奨サイズ/縦横比チェック

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題として記録」「推奨サイズ/縦横比チェック」
- 概要: カバー画像等の推奨サイズ・縦横比をアップロード時にチェック・通知する。

### FI-045: OGP画像生成

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題として記録」「OGP画像生成」
- 概要: ゲーム・アセット・キャラクターのSNSシェア用OGP画像を自動生成または設定可能にする。

### FI-046: カバー画像ギャラリー

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題として記録」「カバー画像ギャラリー」
- 概要: 過去に使用したカバー画像の履歴から選択できるギャラリー。

### FI-047: 画像最適化/圧縮

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題として記録」「画像最適化/圧縮」
- 概要: アップロード時またはサーブ時に画像を最適化・圧縮する。

### FI-048: タグ/ジャンル編集

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「タグ/ジャンル編集」
- 概要: ゲーム・アセット・キャラクターのタグ・ジャンルをUI上から編集する機能。

---

## F. いいね / 素材棚 / 採用 / クレジット概念

### FI-049: Like / Shelf DB分離

- 状態: `要設計`
- ROADMAP出典: `docs/ROADMAP.md` 次にやる候補 3番（Like / Shelf DB 分離）、後回し「いいね / 素材棚 DB・UI 分離（設計は docs/PROJECT_SPEC.md に明文化済み）」
- 概要: 現在の `favorites` がいいねと素材棚を兼任。`AssetLike`/`AssetShelfItem` 等に分離。設計は `PROJECT_SPEC.md` 参照。

### FI-050: AssetLike / CharacterLike

- 状態: `要設計`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「いいね / Like（AssetLike / CharacterLike）」
- 概要: 純粋な好き/応援/評価指標。ランキング・おすすめへの活用を想定。

### FI-051: AssetShelfItem / CharacterShelfItem

- 状態: `要設計`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「素材棚 / Shelf（AssetShelfItem / CharacterShelfItem）」
- 概要: 制作用に保存したアセット/キャラクター。`AssetPicker`/`CharacterPicker` の候補として利用。

### FI-052: Adoption（GameAssetReference / GameCharacterReference）

- 状態: `一部実装`
- ROADMAP出典: `docs/ROADMAP.md` 実装済み「ゲーム使用素材・キャラクター参照DB分離MVP」
- 概要: ゲーム内ノード/シーンで実際に参照されているアセット/キャラクターの採用テーブル。基盤は実装済み。

### FI-053: Credit / GameCredit

- 状態: `実装済み`
- ROADMAP出典: `docs/ROADMAP.md` 実装済み「GameCredit DB 分離MVP」「公開時点スナップショット固定MVP」
- 概要: 公開ゲームページ・スタッフロールでの素材作者/キャラクター作者表示。スナップショット固定実装済み。

### FI-054: 現favoritesの役割分離

- 状態: `要設計`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「段階案: 文言整理MVP → AssetLike / AssetShelfItem 導入 → 構造化ライセンス」
- 概要: 現行 `favorites` を Like/Shelf に段階的に分離する移行計画の確定。

---

## G. エディタ / テストプレイ / ライブ編集

### FI-055: edit画面プロパティフォーム共通コンポーネント化

- 状態: `一部実装 / Phase 1 演出系フォーム共通化MVP実装済み`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「ゲーム編集体験」「edit 画面プロパティフォームの共通コンポーネント化（通常表示/全画面表示の二重実装解消）」
- 概要: 通常表示と全画面表示で二重実装になっているプロパティフォームを共通コンポーネント化。
- 補足: 背景フィルター / 背景ぼかしMVP実装時に「通常表示側のみUI追加、全画面側へ反映漏れ」の再発事例あり。段階的移行方針は `docs/editor-property-form-refactor-plan.md` を参照。
- 実装内容: `apps/frontend/components/editor/NodeEffectsFields.vue` を新規作成。カメラ・カメラ演出・ビジュアルエフェクト・カラーフィルター・背景フィルターを統合。2026-05-15 実装完了。

### FI-056: 右ペインセクション要約表示

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「右ペインセクションの要約表示（閉じた状態での情報把握）の強化」
- 概要: 右ペインのセクションを閉じた状態でも、設定値の概要が分かる要約を表示する。

### FI-057: edit画面情報設計v2（本格的）

- 状態: `要設計`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「edit 画面全体の本格的な情報設計v2」
- 概要: 右ペインセクション化MVP（実装済み）の後続として、edit画面全体の情報設計を本格的に再構成。

### FI-058: テストプレイ高速確認拡張（自動巡回・自動到達確認）

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「テストプレイ高速確認の拡張（分岐までスキップ / 自動巡回・自動到達確認）」
- 概要: 選択肢まで自動スキップ（実装済み）の後続。全分岐を自動巡回し到達可能ノードを確認。

### FI-059: テストプレイ時のみ警告

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「テストプレイ時のみの警告表示（開始地点未設定、存在しない遷移先、参照不可素材/キャラクター）」
- 概要: テストプレイ中に開始地点未設定・壊れた遷移・参照不可素材などを警告表示。

### FI-060: 状態インスペクタ（flags / variables / visitedNodes / affinity）

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「将来の状態インスペクタ（flags / variables / visitedNodes / affinity）」
- 概要: テストプレイ中にゲーム内部状態（フラグ・変数・訪問済みノード・好感度等）をインスペクタ表示。

### FI-061: 高速確認設定の永続化

- 状態: `MVP実装済み`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「高速確認設定の永続化」
- 概要: 作者向けテストプレイの高速確認ON/OFF設定を localStorage (`talking.testPlay.fastConfirm.v1`) に保存・復元する。次回以降の作者向けテストプレイでも ON/OFF 状態が維持される。保存対象は高速確認ON/OFFのみ。全文表示の単発状態・testPlayInstantRevealNodeId・遷移ログは保存しない。

### FI-062: 遷移ログ永続化

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「遷移ログの永続化」
- 概要: テストプレイの遷移ログをlocalStorage等に永続化（現在はセッションメモリのみ最大30件）。

### FI-063: 遷移ログコピー/エクスポート

- 状態: `MVP実装済み`
- ROADMAP出典: `docs/ROADMAP.md` テストプレイ支援MVP「遷移ログコピー/エクスポート」
- 概要: テストプレイパネルの遷移ログをテキストコピー、またはJSONファイルとしてエクスポートできる機能。テキストコピーは見出し・ゲーム情報・日時・ログ件数・各行を含む人間が読める形式。JSONエクスポートは構造化データとして出力し、ファイル名は `talking-transition-log-{gameId}-{timestamp}.json`。永続化は今回スコープ外（メモリ上の最大30件を対象）。

### FI-064: プレイ中編集（ノード作成/選択肢作成）

- 状態: `要設計`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「プレイ中編集」「ノード作成/選択肢作成」
- 概要: テストプレイ画面からノード作成・選択肢追加などの編集操作を行う。

### FI-065: スライド編集パネル

- 状態: `要設計`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「スライド編集パネル」
- 概要: プレイ中にスライドインするサイドパネルで編集を行うUI構想。

### FI-066: ライブ編集 / プレイしながら作るモード

- 状態: `要設計`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「ライブ編集 / プレイしながら作るモード（中長期構想 / P3）」
- 概要: 全画面プレイ + 作者用スライドパネルで再生中にノード作成・選択肢追加・遷移先設定まで行える編集体験v2。

### FI-067: スマホ/タブレット向け編集体験再設計

- 状態: `要設計`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「スマホ/タブレット向け編集体験の再設計」
- 概要: 3ペイン前提のPC向けエディタをモバイルで使いやすくする再設計。

### FI-068: 3ペイン構造再設計

- 状態: `要設計`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「3ペイン構造そのものの再設計」
- 概要: 現在の左ペイン（シーン/ノード）・中央ペイン（プロパティフォーム）・右ペイン（設定）の構造自体を見直す。

### FI-069: 作業位置保存リセット導線

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「作業位置保存のリセット導線（例: 「最後の選択位置をリセット」）」
- 概要: locaStorageに保存した最後の選択位置をUIから明示的にリセットできる導線。

### FI-070: NodePicker左ペインのキーボード操作/フォーカス設計/スクロール保持

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「NodePicker シーン一覧（左ペイン）のキーボード操作・フォーカス設計・スクロール保持」
- 概要: シーン一覧をキーボードで操作可能にし、選択変更後のスクロール位置を保持する。

---

## H. プレイヤー体験

### FI-071: キーコンフィグ

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「プレイヤー体験」「キーコンフィグ・AUTO/Skip 高度化・プレイヤーごとのセーブデータ設計」
- 概要: プレイヤーがキーボードショートカットをカスタマイズできる機能。

### FI-072: AUTO/Skip高度化

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「プレイヤー体験」「AUTO/Skip 高度化」、履歴「AUTO / Skip の高度化」
- 概要: AUTOの速度調整・Skip条件（既読のみ/全て）・Skip終了条件などの高度化。

### FI-073: プレイヤーごとのセーブデータ設計

- 状態: `要設計`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「プレイヤー体験」「プレイヤーごとのセーブデータ設計」
- 概要: 現在のセーブデータはゲームオーナーのみ。プレイヤー個人のセーブデータ機能の設計。

### FI-074: プレイヤーごとの既読ログ保存

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題として記録」「プレイヤーごとの既読ログ保存」
- 概要: Skip機能の既読判定に使用。プレイヤー単位で訪問済みノードを永続化。

### FI-075: スマホ/タブレット向けプレイ操作最適化

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題として記録」「スマホ/タブレット向けプレイ操作最適化」
- 概要: タップ操作・スワイプ・画面サイズに最適化したプレイUI。

### FI-116: クリア後エンドカードMVP

- 状態: `実装済み（2026-05-15）。軽polish 2026-05-16完了`
- ROADMAP出典: `docs/ROADMAP.md` 現在地サマリ「クリア後エンドカードMVP」、`docs/IDEA_BACKLOG.md`「作品クリア後のエンドカード」
- 概要: 公開プレイの終端ノード（`nextNodeId` なし、選択肢なし）で、本文表示完了後の次操作により読了後カードを表示する。カードから「もう一度遊ぶ」「作品詳細へ戻る」「作者プロフィールを見る」「クレジットを見る」「公開ゲームを探す」へ回遊できる。
- 補足: 終端到達直後ではなく、最後の本文を読めるように既存の進行処理（`advanceWithinNodeOrNext()`）で表示する。通常表示と全画面表示のUI境界を崩さないこと。軽polish では背景UI（テストプレイパネル等）の映り込みを抑制し、エンドカードのボタンレイアウトをスマホ幅対応に調整。
- 出典: `apps/frontend/pages/games/[id]/play.vue`, `apps/frontend/components/game/EndCard.vue`
- 後続候補: 作者の他作品表示、お気に入りボタン、感想導線、エンドカード専用一枚絵 / Fin 表示 / エンディング別カード拡張。

---

## I. ゲーム公開 / 一覧 / プロフィール

### FI-076: 公開ゲーム一覧ページネーション/無限スクロール

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「公開ゲーム一覧の拡張（ページネーション / 無限スクロール / …）」
- 概要: 現在の公開ゲーム一覧はシンプルな一覧。ページネーションまたは無限スクロールを導入。

### FI-077: 公開ゲーム一覧 人気順/プレイ数順

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「人気順・プレイ数順」
- 概要: 公開ゲーム一覧の並び替えに人気順・プレイ数順を追加。

### FI-078: 公開ゲーム一覧 タグ検索/作者検索

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「タグ検索 / 作者検索」
- 概要: 公開ゲーム一覧でタグフィルタ・作者名検索を可能にする。

### FI-079: プロフィールslug

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「プロフィール拡張（slug / …）」
- 概要: 現在のプロフィールURLは `/profiles/:userId`（UUID）。`/profiles/:slug` 形式のカスタムURLを設定可能にする。

### FI-080: プロフィール画像

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「プロフィール拡張（… / プロフィール画像 / …）」
- 概要: プロフィールページにアバター画像を設定できる機能。

### FI-081: SNSリンク

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「プロフィール拡張（… / SNSリンク / …）」
- 概要: プロフィールページにX/Twitter・GitHub等のSNSリンクを追加できる機能。

### FI-082: 本格的な作者別作品一覧

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「本格的な作者別作品一覧」
- 概要: 現在のプロフィールは各6件のみ。全件表示・絞り込み・ページネーション対応の本格版。

### FI-083: slug/URL編集（ゲーム）

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題として記録」「slug/URL編集」
- 概要: ゲームURLをカスタムスラッグで設定可能にする。

### FI-084: 作者コメント/あとがき

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題として記録」「作者コメント/あとがき」
- 概要: ゲームの公開ページに作者からのコメント・あとがきを掲載できるフィールド。

### FI-085: 年齢制限/注意書き

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題として記録」「年齢制限/注意書き」
- 概要: ゲームに年齢制限フラグや注意書き（暴力表現・性描写等）を設定し公開時に表示。

### FI-086: 公開設定詳細

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題として記録」「公開設定詳細」
- 概要: 公開設定を ON/OFF の2択以上に拡張（限定公開・パスワード保護等）。

### FI-087: 編集履歴/変更履歴

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題として記録」「編集履歴/変更履歴」
- 概要: ゲームの更新内容を履歴として記録・表示する機能。

### FI-088: 公開済みゲーム変更時の通知

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題として記録」「公開済みゲーム変更時の通知」
- 概要: 公開中ゲームが更新された際にお気に入りユーザー等へ通知する機能。

---

## J. シナリオ / ノード / 制作ツール

### FI-089: 変数条件の厳密評価

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「シナリオ・制作ツール」「変数条件の厳密評価（condition/alternateCondition）」
- 概要: `condition`/`alternateCondition` の評価ロジックを厳密化し、複雑な条件式に対応。

### FI-090: フローチャート可視化

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「シナリオ・制作ツール」「フローチャート可視化」
- 概要: ノード間の遷移関係をフローチャートとして可視化する。

### FI-091: シナリオ Import/Export（JSON → Markdown/DSL）

- 状態: `一部MVP実装済み`
- ROADMAP出典: `docs/ROADMAP.md` 後回しにする大きめ課題「シナリオ・制作ツール」「シナリオ Import/Export（JSON → AI 向け Markdown/DSL）」及び 2026-05-15 実装「AIレビュー用 台本Markdown Export MVP」
- 概要: ゲームシナリオをJSON・Markdown・独自DSLでエクスポート/インポートできる機能。AIレビュー用の最低限の台本Markdownのみ実装済み。Import、JSON Export、DSL、高度な解析付きExportは未実装。

### FI-092: シーン複製

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題として記録」「シーン複製（シーン内ノード/選択肢一括複製、シーン内参照の再マッピング）」
- 概要: シーンとその内部ノード・選択肢を一括複製し、参照を再マッピングする。

### FI-093: ノード複製

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題として記録」「ノード複製（nextNodeId / choice遷移先の扱い方針）」
- 概要: ノードを複製する際の遷移先参照の扱い方針を決定する必要あり。

### FI-094: ノード移動（別シーン間）

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題として記録」「ノード移動（別シーン移動時のID/参照維持方針と startNodeId 影響整理）」
- 概要: ノードを別シーンに移動する機能。ID・参照・startNodeIdへの影響を整理する必要あり。

### FI-095: シーン間ノードコピー

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題として記録」「シーン間ノードコピー（コピー先で新nodeId採番、参照ポリシー整理）」
- 概要: ノードを別シーンにコピーする機能。コピー先で新 nodeId を採番する。

### FI-096: undo/redo

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題として記録」「付随課題: undo/redo」
- 概要: エディタ上の操作をundo/redoできる機能。設計課題が大きく要設計。

### FI-097: 操作前確認ダイアログ（コピー先選択UI）

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題として記録」「操作前確認ダイアログ、コピー先選択UI」
- 概要: 複製・移動・コピー時のターゲット選択UIと操作前確認ダイアログ。

### FI-098: 大量ノード操作

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題として記録」「大量ノード操作」
- 概要: ノードの一括選択・一括削除・一括移動などの大量操作機能。

### FI-099: シーン/ノードテンプレート化

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題として記録」「シーン/ノードテンプレート化」
- 概要: よく使う構造のシーン/ノードをテンプレートとして保存・再利用する機能。

---

## K. 参照バリデーション / 参照診断

### FI-100: characterId（speakerCharacterId）が本人所有 or お気に入り済みかの確認

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題（ゲーム内参照素材）」「characterId（speakerCharacterId）が本人所有 or お気に入り済みかの確認」
- 概要: ノード保存時に `speakerCharacterId` の所有・お気に入り状態をサーバ側で検証する。

### FI-101: characterImageId / portraitAssetId が選択済みキャラクターに属するか

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題（ゲーム内参照素材）」「characterImageId / portraitAssetId が選択済みキャラクターに属するかの確認」
- 概要: ノード保存時に立ち絵アセットが指定キャラクターに属しているかをサーバ側で検証。

### FI-102: キャラクター配置全体の参照バリデーション

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題（ゲーム内参照素材）」「キャラクター配置全体の参照バリデーション（キャラ配置ブロックまとめて検証）」
- 概要: キャラクター配置ブロック全体をまとめて検証する共通バリデーション。

### FI-103: 参照切れ素材のシナリオチェック warning

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` 履歴「将来課題（ゲーム内参照素材）」「参照切れ素材のシナリオチェック warning（bg/BGM/SE/キャラ全体）」
- 概要: シナリオチェックで背景/BGM/SE/キャラクター全体の参照切れをwarningとして表示。

### FI-104: アセット削除/非公開化時の利用中ゲームへの影響表示

- 状態: `一部実装`
- ROADMAP出典: `docs/ROADMAP.md` 実装済み「Asset/Character 非公開化時の利用影響表示MVP」、履歴「アセット削除/非公開化時の利用中ゲームへの影響表示」
- 概要: 削除/非公開化時の影響表示は実装済み。後続: 利用中ゲームの詳細一覧・修正ジャンプ等。

---

## L. インフラ / ビルド / 運用

### FI-105: Prisma EPERM（query_engine DLLロック問題）

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` ビルド状態テーブル（2026-05-02〜）「apps/api prisma:generate で query_engine-windows.dll.node rename 時に EPERM（DLLロック）」
- 概要: Windows環境で `prisma:generate` 実行時にDLLがロックされEPERMエラーが頻発。根本的な回避策が必要。

### FI-106: apps/api test script 未定義

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` ビルド状態テーブル「pnpm -C apps/api run test は ERR_PNPM_NO_SCRIPT（test script未定義）」
- 概要: `apps/api/package.json` に `test` スクリプトが定義されていない。E2Eテストの整備が必要。

### FI-107: @nuxt/icon Nuxt 4互換警告

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` ビルド状態テーブル「WARN: @nuxt/icon Nuxt 3.19.3 非互換（>=4.0.0 必要）」
- 概要: `@nuxt/icon` がNuxt 4互換バージョンを要求。アップデートまたは代替対応が必要。

### FI-108: browserslist 更新

- 状態: `未着手`
- ROADMAP出典: `docs/ROADMAP.md` ビルド状態テーブル「browserslist 7ヶ月古い（軽微）」
- 概要: browserslistのデータが7ヶ月以上古い旨のWARN。`npx update-browserslist-db@latest` で更新。

### FI-109: worker build timeout

- 状態: `要確認`
- ROADMAP出典: ユーザー指摘。`apps/worker/` はワークスペースに存在するが、ROADMAPのビルドログに "worker build timeout" の明示的な記載は確認できなかった。
- 概要: workerビルドがタイムアウトする問題。出典・詳細を要確認。

### FI-110: Meilisearchインデックス運用強化

- 状態: `後続拡張`
- ROADMAP出典: `docs/ROADMAP.md` 実装済み「RUNBOOK / migration / Meili手順整理MVP」、後回し「アセット指標の検索連携（Meilisearch 連携）」
- 概要: 手順整理は完了。後続: インデックス再構築の自動化・障害復旧フロー強化。

### FI-111: CI安定化

- 状態: `要設計`
- ROADMAP出典: `docs/ROADMAP.md` ビルド状態テーブル（EPERMやAPI testスクリプト未定義等の既知課題）
- 概要: Windowsのビルド環境・テスト環境を安定させるCI整備。EPERM対策・テストスクリプト整備・lint/typecheck自動化。

---

## M. 要確認

### FI-112: コメント/レビュー機能（一般ユーザーによるコメント投稿）

- 状態: `要確認`
- ROADMAP出典: ROADMAPに「コメント機能」としての明確な出典なし。「作者コメント/あとがき」（FI-084）とは別概念。旧 FI-003 として登録されていたが出典が確認できない。
- 概要: ゲームや素材に対する一般ユーザーのコメント投稿・表示・モデレーション機能。ROADMAPへの明記を確認の上、要否を判断すること。

### FI-113: ゲーム内フラグ/変数システム本格実装

- 状態: `要確認`
- ROADMAP出典: FI-060「状態インスペクタ」の対象として `flags / variables` が言及されているが、フラグ/変数システム自体の本格実装がROADMAPで独立した課題として明示されているか不明。
- 概要: ゲームノードにフラグ・変数の設定/評価機能を追加する本格実装。`condition`/`alternateCondition` の評価強化（FI-089）と関連。

### FI-114: 編集画面の表示設定リセット導線

- 状態: `MVP実装済み`
- ROADMAP出典: `docs/ROADMAP.md` ゲーム制作/編集基盤まわり「edit 画面の表示設定リセット導線MVP」
- 概要: 編集画面から 3ペイン幅（`gameEditorPaneWidths`）、右ペインセクション開閉（`talking.editor.rightPaneSections.v1`）、現在ゲームの最後の選択位置（`talking.editor.lastSelection.v1:${gameId}`）をリセットできる導線。localStorage 由来の表示崩れや作業位置復元ミスから復旧しやすくする。ゲーム内容は変更しない。

### FI-115: この素材/キャラクターが使われている作品

- 状態: `MVP実装済み`
- ROADMAP出典: `docs/ROADMAP.md` この素材/キャラクターが使われている作品MVP
- 概要: 素材詳細/キャラクター詳細から、その素材/キャラクターを参照している公開ゲームを最大6件表示する。`GameAssetReference` / `GameCharacterReference` を使い、非公開ゲームは表示しない。詳細な参照位置・ランキング・ページネーションは未実装。

### FI-117: 制作ガイドカードMVP

- 状態: `実装済み（2026-05-16 軽polish含む）`
- ROADMAP出典: `docs/ROADMAP.md` 制作ガイドカードMVP
- 概要: 編集画面右ペインに、開始地点・ノード数・カバー画像・公開前チェックなどから次のアクションを案内する制作ガイドを表示する。折りたたみ・非表示・再表示に対応し、右ペインの圧迫を抑える。
- 補足: 軽polish では縦幅抑制・完了状態表示コンパクト化・余白削減・ボタン配置最適化・補足文言出すぎ抑制を実装。
- 出典: `apps/frontend/pages/my/games/[id]/edit.vue`

---

## 棚卸し対象セクション（参照元）

- `docs/ROADMAP.md` 現在地サマリ（2026-05-15）
- `docs/ROADMAP.md` 実装済み（主要）
- `docs/ROADMAP.md` 🎯 次にやる候補
- `docs/ROADMAP.md` 🔮 後回しにする大きめ課題
- `docs/ROADMAP.md` 🏗️ ビルド状態（既知課題）
- `docs/ROADMAP.md` 📚 履歴ログ内の「将来課題として記録」（2026-05-04 ゲームカバー画像MVP、ゲーム基本情報編集MVP、ゲーム複製MVP）
- `docs/ROADMAP.md` 公開・クレジットまわりの現在仕様「将来課題」